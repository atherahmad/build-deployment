{
  "homepage": "https://atherahmad/github.io//build-deployment",
  "name": "boilerplate",
  "version": "1.0.0",
  "description": "This is a boilerplate",
  "scripts": {
    "start": "parcel src/index.html --dist-dir dev-serve",
    "build": "parcel build src/index.html --dist-dir build --public-url ./",
    "publish": "gh-pages -d build"
  },
  "author": "dci",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "gh-pages": "^4.0.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "bootstrap": "^5.2.0"
  }
}
